# Verification: S028 candidate freeze

## Blocking Spec Kit analysis

Installed specify/clarify/checklist/plan/tasks prerequisites completed. Analysis inspected all 15 functional requirements, 7 success criteria, 3 user stories/10 acceptance scenarios, 29 ordered tasks, 7 constitutional principles and the plan decisions. Requirements-to-task coverage is 100 percent; no orphan task, uncovered requirement, ambiguous material outcome, constitutional contradiction or critical/high finding remains. Task T004 is complete and implementation is authorized.

Clarification coverage is Clear across functional scope, domain identity/lifecycle, interactions/error states, quality/security/privacy, integrations, edge cases, constraints, terminology and completion signals. Four routine decisions were resolved under explicit autopilot, with zero user questions. Built-in requirements checklist is 16/16 passing; all ten custom requirements-quality criteria are explicitly specified while their reviewer-owned markers remain unchanged.

Installed plan scaffolding precedes domain checklist solely because installed prerequisites require plan.md. Incorrect local feature.json key was corrected to feature_directory before successful prerequisite rerun. No extensions.yml/hooks exist.

## Implementation and convergence

Exact current identity is 1.1.0. Official native output declares stable complete capabilities; precise schema-only and complete experimental loaded observations remain accepted without mutation. Development/unknown/mismatched identities reject. Canonical and immutable candidate schema SHA-256 is `223b61cbcf6337167039268576b2c739564585fff10e6cc6076e47a03526a0f7`. The normative schema inventory differs only in three identity locations and the additional complete stable capability tuple.

All seventeen ratified scripted matrix rows now carry executable evidence. Tests reject substituted/duplicate/unknown rows and every deferral. Stable conformance exercises all four native formats, exact restoration, canonical deterministic ASS/SSA rendering, loaded capability observations and forced unsafe-output refusal.

Independent candidate/documentation/release owners completed bounded test-first work. Coordinating review identified two package-proof gaps (complete stable producer/format/capability assertions and discriminating old-consumer identity refusal); both were fixed and verified. Cross-owner audit found one stale architecture claim, now corrected, and has zero unresolved findings.

Installed converge prerequisites completed successfully after implementation. Read-only assessment covered 15 functional requirements, seven success criteria, ten acceptance scenarios, all 29 tasks, ten recorded plan decisions and seven constitutional principles. No missing, partial, contradictory or unrequested buildable obligation remains; no convergence task was appended. T026-T029 are explicitly pending exact-revision/external delivery gates, already fully specified rather than missing implementation.

## Foreground local verification

- Complete root Go tests and vet passed on the Go 1.25.0 compatibility baseline.
- Root and all six standalone modules passed tests, vet, Staticcheck v0.7.0 and govulncheck v1.8.0. Checks found zero called vulnerabilities; the existing unreachable imported advisory remains unchanged.
- All six CGO-disabled Windows/macOS/Linux amd64/arm64 builds passed.
- All sixteen fixed-work fuzz targets completed at least 1,000 executions each with one worker.
- Final release-verifier tests passed after identity-probe contract additions; focused current/historical/schema/model/native/convert/CLI/conformance and observation tests passed.
- Workflow lint, full Go formatting, repository publication formatting, documentation and brand gates passed. Documentation verifier checked 25 documents, 131 local links, ten registered examples and 34 format rows; brand verifier checked 265 entries and six references.
- Full Site lint/generation/drift, 79 unit tests, production build, browser, artifact and Wrangler dry-run passed. Browsers: 78 passed and six intentional skips. The artifact declared 23 routes and exactly two public immutable schemas. This precommit preview identifies baseline 351036c; hosted proof must identify the final candidate head.
- ASS/SSA preview/navigation is present. Published downloads remain v1.0.0; no v1.1.0 public schema route, tag, release, deployment or production change was created.
- GoReleaser v2.18.1 validated publication-disabled candidate configuration; Syft module build information binds v1.51.1.

## Fidelity and published-consumer evidence

Historical v0.0.0/v1.0.0 schemas, bundled historical resources, original source fixtures/manifest and historical inspection goldens are unchanged from S027 main. Current examples are intentionally promoted; source truth is retained.

Explicit published Windows amd64 consumer proof passed using the actual source/digest-bound v1.0.0 archive from the S020 contract, revision `2cad4c816340404289b4d1d87179a4071713bb46`. Four fresh candidate outputs, two frozen historical positive/negative workflows, 32 old-consumer owning runtime/no-publication refusal paths and 32 exact identity probes passed. Input bytes remained unchanged; forced destinations retained sentinels and absent destinations/staging remained absent.

Released inspect suppresses detailed schema errors. Each owning old-command refusal is preceded by a validate identity gate on the same unchanged payload, requiring a historical schema-identity diagnostic. Generic runtime failure alone cannot satisfy compatibility. Counts distinguish identity probes from owning refusals.

## Exact candidate and external delivery gates

Commit cleanly before six-target packaging and matching Windows host proof, then publish the authorized official PR closing #64/#65. Exact source-bound bundle hashes/native proof, hosted native/security/quality/Site checks and terminal reviews will be recorded in a formatted/read-back PR completion comment after arrival, avoiding an evidence-only change to the reviewed head.

Final merge and #66/#67 tag/release/public-hosting work remain under separate operator authority; epic #51 and the milestone remain open.
