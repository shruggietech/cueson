# Data model: S028 frozen candidate

No new Cue JSON fields or native grammar ownership are introduced. The exact current identity becomes 1.1.0; historical 1.0.0 resources/semantics and all original source bytes are immutable.

Current scripted declared support has three precise observations: schema_only (no native ingest/render, restore true), experimental native and stable native (ingest/render/restore true). All reject OCR-required semantics. Official current encode/private native projections use stable; loaded declarations remain unchanged.

The immutable candidate schema is byte-identical to canonical/emitted/embedded/package bytes. Candidate evidence binds version/intended tag, exact full source revision, schema/legal digests, six ordered target archive/SBOM digests, checksum counts, truthful matching host workflow/old-consumer evidence and published false. No verification timestamp or local path becomes public evidence.

Lifecycle: #64 Ready -> Specced -> In progress -> PR review; #65 joins implementation after frozen contract acceptance. Both become Done only after operator merge. #66/#67 and epic/milestone remain pending publication/hosting authority.
