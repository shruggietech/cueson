# S027 data and ownership model

## Inspection summary

The existing inspection report stays version 1. An optional scripted object appears only for ASS/SSA documents and contains fixed numeric aggregate counts of validated sections, records, declarations, styles, events/dialogues/comments, overrides/drawings and attachment observations. Exact fields are fixed by contracts/cli-conformance.md and code/test agreement. No native names, text, attachment bytes or paths are projected. Existing document/block/cue fields retain their meaning and text reports retain their shape.

## Immutable fuzz source

Direct codec parsing creates current typed/native model and summaries. A fixed safe basename, exact byte length, SHA-256 and canonical base64 identify original bytes; timestamps truthfully report unavailable. Encoding/BOM/line-end observations come from supported decoding/parser authority. This is test-only data construction and never a public schema/API bypass. Models validate before conversion.

## Structured mutations

Each callback clones a baseline without rewriting seed/source truth. Accepted edit controls remain valid; known-invalid owner/reference/capture/envelope/privacy mutations refuse at the owning boundary. Callback input and generated work remain bounded.

## Conformance evidence and lifecycle

One matrix row links existing source fixtures, named unit/fuzz/native evidence and explicit inapplicable or later stable/release gates. The decoder accepts exactly one JSON value. #62/#63 proceed Specced → In progress → PR review; Done waits for human merge. Epic/milestone stay open.
