## Outcome

The completed scripted capability and compatibility promise are accurately documented and ready for stable promotion.

## Context

Public schema, CLI, format pages, conversion matrix, and generated site content must agree with executed implementation evidence.

## Scope

Update architecture, compatibility, schema, ASS/SSA grammars, CLI, conversion, installation/download planning, support tables, examples, and changelog. Promote support only after the acceptance gates pass.

## Acceptance criteria

- [ ] ASS/SSA support labels exactly match implemented and verified scope, including drawings, attachments, overrides, karaoke, and all documented conversion edges.
- [ ] The historical-input contract, new-output identity, render-versus-restore semantics, losses, defaults, and limits are explicit.
- [ ] Schema annotations and public examples match the final contract and validate against their target version.
- [ ] Maintained root docs remain the authored authority; generated site content is reproducible and its routes/links/support tables are checked.
- [ ] The changelog records additions and architecture decisions; deferred future families are not advertised as supported.
- [ ] Blocking Spec Kit analysis and convergence have no unresolved material finding.

## Dependencies

Native blockers: [#63](https://github.com/shruggietech/cueson/issues/63). Coordination across the same implementation slice does not add reciprocal dependencies. Protected release/tag and production transitions require their own explicit operator authority.

## Verification

Run documentation verifier, schema examples/annotation tests, generated-content drift checks, site unit/browser/build/artifact gates, formatting/encoding/link checks, and contract conformance.
