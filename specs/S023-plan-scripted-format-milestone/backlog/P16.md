## Outcome

The public site accurately presents v1.1.0 and serves its immutable schema while retaining every older schema route.

## Context

The public site is already live; each additional released schema requires explicit route/inventory coverage and an exact-main deployment.

## Scope

Update generated release/download/support/schema surfaces and the immutable route inventory, prove the artifact, then deploy the reviewed exact main revision only under explicit production authorization. Independently verify public behavior and close milestone coordination.

## Acceptance criteria

- [ ] Public documentation and download links identify the verified v1.1.0 release without dropping older releases.
- [ ] The new schema route serves byte-identical immutable v1.1.0 bytes; v0.0.0/v1.0.0 routes remain byte-identical and no mutable latest alias exists.
- [ ] Build, generation drift, browser/accessibility, link, route, metadata, schema-byte, and Wrangler dry-run checks pass.
- [ ] The authorized deployment uses the exact reviewed revision; DNS, trusted TLS, www redirect, all declared routes, downloads, deployment metadata, and three schema hashes pass live verification.
- [ ] Unrelated production state remains preserved and mutation/read-back evidence is recorded.
- [ ] Only after every scoped child passes verification are the epic and milestone closed, native dependencies reconciled, Project stages Done, and default Status cleared.

## Dependencies

Native blockers: [#66](https://github.com/shruggietech/cueson/issues/66). Coordination across the same implementation slice does not add reciprocal dependencies. Protected release/tag and production transitions require their own explicit operator authority.

## Verification

Run the complete site suite and artifact proof; inspect production before mutation, deploy the selected approved revision, and run trusted live DNS/HTTP/TLS/content/hash checks with transparent environment-specific evidence.
