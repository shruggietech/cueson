# Quickstart: Validate the protected production deployment repair

## Review the governed inputs

Read the constitution, architecture production contract, issue #78, the S032 specification, plan, credential research, and protected deployment contract. Confirm the branch started from exact current `origin/main` and production is healthy before changing workflow behavior.

## Demonstrate the command-boundary defect

Add the focused package-script subprocess tests first. Run them against the existing workflow contract and record that the Cloudflare verifier receives an invalid standalone separator.

## Implement and verify locally

Remove the standalone separators, bind dispatch ref and SHA to the requested revision, scope the token to three Cloudflare-facing steps, and document the durable credential lifecycle. Run focused workflow and command-boundary tests, then the complete site and repository verification gates.

## Publish for review

Commit the converged source, push the authorized branch, and open one official pull request that closes the S032 implementation issue and references #78. Wait for every required check and first-round Codex/security result. Address all findings and request at most one second Codex review if round one reports issues.

## Complete production proof after human merge

Provision the reviewed protected credential, dispatch the merged workflow from `main` with the exact current-main revision, and verify the run plus independent public and Cloudflare evidence. Close #78 only after every production acceptance criterion is proven. The agent does not merge the pull request.
